spring.application.name=uberApp


#DB configuration
spring.datasource.url=jdbc:postgresql://${DB_HOST_URL}:5432/${DB_NAME}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=update

#Security config
jwt.secretKey = ${JWT_SECRET_KEY}

# SMTP config
spring.mail.username=souravkumarverma2004@gmail.com
spring.mail.password=qqix squy xqzr swge  # (app password, correct)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.transport.protocol=smtp